import React from 'react';

const Header = () => {
    return (
        <header>
            <div className={'container'}>
                <h1>This header</h1>
            </div>
        </header>
    );
};

export default Header;
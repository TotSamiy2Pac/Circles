import React from 'react';

const Sign = () => {
    return (
        <div>
            <div>
                <button>Sign In</button>
                <button>Sign Up</button>
            </div>
        </div>
    );
};

export default Sign;